import avatar from '../assets/avatar.png'

// Hardcoded to simulate a data source, e.g. redux store
const user = {
  notificationCount: 5,
  firstName: 'Monica',
  lastName: 'Simons',
  avatar
}

export default user
